"""Heuristic term detection → pin dicts. Track C implements this."""


def detect_pins(words_doc: dict, *, max_per_page: int = 12) -> list[dict]:
    raise NotImplementedError("Track C implements detect_pins()")
